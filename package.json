{
  "name": "wraith",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "keccak",
      "secp256k1",
      "bufferutil",
      "utf-8-validate",
      "sharp"
    ]
  }
}
